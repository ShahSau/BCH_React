import React from "react";

const About = () => {
  return (
    <div>
      this is a school project using react Router Dom(Link, NavLink, routing,
      params, switch)
    </div>
  );
};

export default About;
